---
const { text, dark } = Astro.props;
---
<button class={`text-lg py-2 px-8 rounded
	${dark ? 'bg-slate-900 hover:bg-slate-700 text-white' : 'bg-white hover:bg-slate-100 border border-slate-300'}
	`}>
	{text}
</button>
